METADATA:

../data/ 
contains CSV and JSON files (raw, processed) plus metadata .txt

../data/raw/ 
contains

--------------------------------------------------------------------------
inside a notebook: !pip install -r requirements.txt
in git Bash: pip install -r requirements.txt
